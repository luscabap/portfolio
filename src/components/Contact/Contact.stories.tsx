import type { Meta } from "@storybook/react";
import Conctact from "./Contact";

const meta: Meta<typeof Conctact > = {
  component: Conctact
};

export default {
  title: "Components/Contact",
  component: Conctact
};

export const Default = {}