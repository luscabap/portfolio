export type ProjectProps = {
  title: string,
  description: string,
  subtitulo: string,
  img: string,
  techs: string[],
  deploy: string,
  repo: string,
  repoName: string,
  concluido: string,
  primario?: string,
}